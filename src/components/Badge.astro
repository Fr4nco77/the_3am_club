---
interface Props {
  class?: string;
}
const { class: extraClass } = Astro.props;
---

<div
  class:list={[
    "cursor-default rounded-full border px-3 py-0.5 text-sm font-medium tracking-wider text-white shadow-lg backdrop-blur-sm transition-transform duration-300 ease-in-out hover:scale-105",
    extraClass,
  ]}
>
  <slot />
</div>
